# Web layer
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
aiofiles>=23.2.1
jinja2>=3.1.0
itsdangerous>=2.1.0
email-validator>=2.1.0
httpx>=0.26.0

# Data / persistence
sqlalchemy>=2.0.25
pydantic-settings>=2.1.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<4.1

# Inference — ONNX Runtime only. The YOLOv5 weights are exported to ONNX
# (models/best.onnx), so PyTorch/ultralytics are NOT needed at runtime.
# That keeps the image ~2 GB smaller, the build fast, RAM low, and removes
# the checkpoint-unpickling issues that broke earlier deploys.
onnxruntime>=1.16.0
numpy>=1.26.0
opencv-python-headless>=4.8.0
pillow>=10.2.0
PyYAML>=6.0

# Reporting
reportlab>=4.0.0
